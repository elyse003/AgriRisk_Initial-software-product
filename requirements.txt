# Core app + ML, pinned to the versions the shipped models were trained/pickled
# with, so they unpickle correctly on Streamlit Cloud (a scikit-learn mismatch
# would otherwise break model loading).
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
requests
python-dotenv

# database: PostgreSQL in production via DATABASE_URL, SQLite locally.
# psycopg3 binary wheel installs reliably on Streamlit Cloud (psycopg2-binary
# can fail to build there and break the whole install).
sqlalchemy>=2.0
psycopg[binary]>=3.1

# data rebuild (scripts/download_data.py + prepare_data.py read Excel sources)
openpyxl
xlrd

# tests (not needed to run the app)
pytest
Authlib>=1.3.2          # Google sign-in (OpenID Connect) via st.login()
