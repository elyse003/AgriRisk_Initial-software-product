# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/

# env / secrets
.env

# The cleaned processed data (small, real) and trained models ARE committed so
# the app clones-and-runs on real data with no rebuild step. Both are regenerable
# via scripts/download_data.py -> prepare_data.py -> train_models.py.

# Large raw source downloads are NOT committed (regenerate with download_data.py):
data/raw/wfp_food_prices_rwa.csv
data/raw/rwa-rainfall-subnat-full.csv
data/raw/CMO-Historical-Data-Monthly.xlsx
data/raw/worldbank_cpi_rwanda.csv

# mlflow
mlruns/

# notebooks
.ipynb_checkpoints/

# OS
.DS_Store

# runtime database (auto-created)
data/agririsk.db
*.db
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
branch_structure.json
data/feedback_export.csv
