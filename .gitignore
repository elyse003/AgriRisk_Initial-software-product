# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/

# env / secrets
.env

# processed data (regenerated) and real downloads
data/processed/*
!data/processed/.gitkeep

# NOTE: the small synthetic sample CSVs in data/raw/ ARE committed so the repo
# runs out of the box. Replace them with real data locally (kept out via size).

# trained models (regenerated by scripts/build_notebook.py)
models_store/

# mlflow
mlruns/

# notebooks
.ipynb_checkpoints/

# OS
.DS_Store
